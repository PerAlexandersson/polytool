[package]
name = "sym-poly-core"
version = "0.1.0"
edition = "2021"
description = "Shared foundation for symmetric polynomial algebras: Ring trait, partitions, compositions, basis indexing, formal sums, transition matrix caching"
readme = "README.md"

[dependencies]
combinatoric-core = { path = "../../combinatoric-core" }
num-bigint = "0.4"
num-rational = "0.4"
num-traits = "0.2"
num-integer = "0.1"
